import Channel, { ChannelData } from '../Channel/';
import Twitch from '../../';
export interface ChannelEventData {
    _id: string;
    start_time: string;
    end_time: string;
    time_zone_id: string;
    title: string;
    description: string;
    cover_image_url: string;
    language: string;
    channel: ChannelData;
}
export interface ChannelEventAPIResult {
    _total: number;
    events: ChannelEventData[];
}
export default class ChannelEvent {
    private readonly _data;
    _client: Twitch;
    constructor(_data: ChannelEventData, client: Twitch);
    readonly channel: Channel;
    readonly id: string;
    readonly startTime: Date;
    readonly endTime: Date;
    readonly title: string;
    readonly description: string;
    buildCoverImageUrl(width: number, height: number): string;
}
