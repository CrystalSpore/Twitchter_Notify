"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var UserTools = /** @class */ (function () {
    function UserTools() {
    }
    UserTools.getUserId = function (user) {
        return typeof user === 'string' ? user : user.id;
    };
    UserTools.getUserName = function (user) {
        return typeof user === 'string' ? user : user.name;
    };
    UserTools.toChannelName = function (user) {
        // remove leading pound first - in case it already is a channel name
        return "#" + this.toUserName(user);
    };
    UserTools.toUserName = function (channel) {
        // it's okay if this is already a user name, we only remove the first character if it's a pound
        return channel.replace(/^#/, '').toLowerCase();
    };
    return UserTools;
}());
exports.default = UserTools;
//# sourceMappingURL=UserTools.js.map