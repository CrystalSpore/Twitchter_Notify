import Twitch from '../../../';
import HelixUser from '../User/HelixUser';
export declare type HelixStreamType = '' | 'live' | 'vodcast';
export interface HelixStreamData {
    id: string;
    user_id: string;
    game_id: string;
    community_ids: string[];
    type: HelixStreamType;
    title: string;
    viewer_count: number;
    started_at: string;
    language: string;
    thumbnail_url: string;
}
export default class HelixStream {
    private readonly _data;
    private readonly _client;
    constructor(_data: HelixStreamData, client: Twitch);
    getUser(): Promise<HelixUser>;
    readonly viewers: number;
    readonly startDate: Date;
    readonly type: HelixStreamType;
}
